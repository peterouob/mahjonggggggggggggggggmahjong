name: mahjoin_frontend
description: "Find nearby Mahjong players in real-time."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  go_router: ^14.6.2
  flutter_riverpod: ^2.6.1
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  geolocator: ^11.0.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
